language: java
jdk:
  - oraclejdk11
addons:
  sonarcloud:
    organization: "charlemaznable"
script:
  - mvn clean verify sonar:sonar -Dsonar.projectKey=CharLemAznable_embedded-mysql-extension
cache:
  directories:
    - '$HOME/.sonar/cache'
    - '$HOME/.embedmysql'
after_success:
  - bash <(curl -s https://codecov.io/bash)
